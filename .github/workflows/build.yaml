name: Build and deploy

on:
  push:
    branches: [main]
  #workflow_dispatch:

jobs:
  build:
    name: Build and Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-central-1

      - name: Predict cost
        uses: salim8898/AWS-Cost-Predictor@v1.0.3
        with:
          iac_path: infra    # path of IAC folder in your repo
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      # - name: Deploy to S3 bucket
      #   run: |
      #     echo "Checking S3 bucket exists..."
      #     BUCKET_EXISTS=true
      #     S3_CHECK=$(aws s3 ls "s3://${{ secrets.S3_BUCKET }}" 2>&1)

      #     #Some sort of error happened with s3 check
      #     if [ $? != 0 ]
      #     then
      #       NO_BUCKET_CHECK=$(echo $S3_CHECK | grep -c 'NoSuchBucket')
      #       if [ $NO_BUCKET_CHECK = 1 ]; then
      #         echo "Bucket does not exist"
      #         BUCKET_EXISTS=false
      #       else
      #         echo "Error checking S3 Bucket"
      #         echo "$S3_CHECK"
      #         exit 1
      #       fi
      #     else
      #       echo "Bucket exists"
      #     fi
